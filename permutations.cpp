#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> ar(n);

    for (int i = 0; i < n; i++){
        cin >> ar[i];
    }

    vector<vector<int> > pms;

    do{
        // code here

    }
    while (next_permutation(ar.begin(), ar.end()));
}
